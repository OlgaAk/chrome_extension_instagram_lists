chrome.runtime.sendMessage({
    todo: "showPageAction"
})

// var div = document.querySelector(".PZuss")
// console.log(div)
// var subscribers = div.querySelectorAll('li')
// subscribers.forEach(function (subscriber) {
//     subscriber.style.color = "red"
// })

// find a tag with given title and hide whole article which contains it
// document.querySelector("a[title=spb_export_center]").closest("article").style.display = "none"

// get subscribers username from json, max 12 nodes
// data.user.feed_reels_tray.edge_reels_tray_to_reel.edges.forEach(function (node) {
//     console.log(node.node.owner.username)
// })